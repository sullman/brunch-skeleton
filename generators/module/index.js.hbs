/* jshint node: true */
"use strict";

var Backbone = require("backbone");
var app = require("application");
var _ = require("underscore");
var AppRouter = require("utils/router");
var Marionette = Backbone.Marionette;

var {{#camelize}}{{name}}{{/camelize}} = app.module('{{#camelize}}{{name}}{{/camelize}}');
var Views = require("./views");
var Models = require("./models");

var Router = AppRouter.extend({
});

var Controller = Marionette.Controller.extend({
  start: function () {
  }
});

{{#camelize}}{{name}}{{/camelize}}.addInitializer(function () {
  var controller = new Controller();
  controller.router = new Router({
    controller: controller
  });

  controller.start();
  {{#camelize}}{{name}}{{/camelize}}.controller = controller;
});

module.exports = {{#camelize}}{{name}}{{/camelize}};
